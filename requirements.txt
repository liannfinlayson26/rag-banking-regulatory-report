# RAG over a banking regulatory document — pinned top-level dependencies.
# Generated against Python 3.13. Transitive deps are resolved by pip.
# Re-pin with:  ./venv/bin/pip freeze > requirements.lock.txt

# --- Orchestration / LLM framework ---
langchain==1.3.7
langchain-text-splitters==1.1.2
langgraph==1.2.4

# --- Model providers ---
langchain-openai==1.3.0
langchain-google-genai==4.2.5

# --- Vector store ---
chromadb==1.5.9
langchain-chroma==1.1.0

# --- Web search (retrieval augmentation) ---
tavily-python==0.7.26

# --- Config / data models ---
python-dotenv==1.2.2
pydantic==2.13.4

# --- Table-aware PDF parsing ---
# pdfplumber: general text + word/line geometry extraction.
# camelot-py: dedicated table extraction; v2.0 pulls in opencv-python-headless
#   and is best for BORDERED (lattice) tables via its ruling-line detection.
#   Requires the Ghostscript system binary (`brew install ghostscript`).
pdfplumber==0.11.9
camelot-py==2.0.0

# --- Notebooks (dev tooling, needed to run/execute *.ipynb) ---
ipykernel==7.3.0
nbconvert==7.17.1

# --- Interactive demo (optional; only needed for `streamlit run app.py`) ---
# app.py wraps the same compiled agent and streams the reasoning trace live.
# Not required for the notebooks or `python eval.py`. Install with:
#   pip install streamlit
streamlit>=1.40
